# Endpoints

- `GET /` estado general
- `GET /health` salud del servicio
- `POST /auth/login` autenticacion del operador unico
- `POST /alumnos/registrar` registro de alumno
- `POST /becas/registrar` registro de beca
- `POST /imss/registrar` registro IMSS
