# Seguridad

- Autenticacion por `X-API-Key` o `Authorization: Bearer <token>`.
- El sistema usa un solo usuario operador configurado en `.env`.
- Validacion de campos obligatorios.
- Respuestas JSON uniformes.
- CORS controlado desde configuracion.
